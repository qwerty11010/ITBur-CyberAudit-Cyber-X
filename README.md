# ITBur-CyberAudit-Кибер-Х

# *** ПРОСЬБА ПРОЧИТАТЬ! Это важно. ***

Привет! Эта программа написана в среде разработки Visual Studio Code. Пользовались помощью из Всемирной Сети (Интернет) и также лекциями. На GitHub русский язык не пропускает, поэтому в названии репозитория название команды указано на английском (Cyber-X).

**Участники команды:**
1. Павлова Алиса Игоревна, 11 класс (капитан)
2. Надмитова Сэлмэг Евгеньевна, 9 класс
3. Пельменев Кирилл Алексеевич, 9 класс

Связь с **капитаном** (TG): @zudybe

**Описание:**
Консольное Python-приложение для базового аудита безопасности Linux (Kali/Debian/Ubuntu).  Что проверяет и делает: права доступа, сеть, пакеты/версии, простые рекомендации. Сохраняет отчет в "report.json".


**Требования к системе:**

-OS: Linux (Kali/Debian/Ubuntu)

-Python: 3.10+ (обычно подойдет любой Python 3)

-Встроенные команды: "find", "ss", "dpkg-query" (на Kali обычно есть)

**Зависимости:**

Внешних Python-зависимостей нет (см. "requirements.txt").


# Скачивание репозитория:
```bash
git clone 
```

# Установка:
Если нужно, создайте виртуальное окружение (необязательно):

```bash(ч/з терминал)
python3 -m venv .venv
source .venv/bin/activate
```

# Установка зависимостей (ничего не установит, файл для формы):

```bash
pip install -r requirements.txt
```

# Запуск:
Рекомендуется запускать с правами "sudo", иначе часть проверок может быть неполной!:

```bash
sudo python3 audit.py
```

# Также можно без "sudo":

```bash
python3 audit.py
```

**Что делает:**

-Аудит прав доступа:

  *поиск файлов с правами "777" и "666" в "/etc", "/var", "/home"
  
  *простой поиск «секретов» по ключевым словам в world-readable файлах
  
-Сетевой аудит:

  *просмотр слушающих портов через "ss -tulpn"
  
  *сверка с небольшой «базой знаний» (Telnet/FTP/SMB/MySQL)
  
-Аудит пакетов:

  *сбор версий пакетов через "dpkg-query"
  
  *выделение популярных сетевых пакетов

**Отчет:**

-Консоль: краткий список проблем + рекомендации

-JSON: "report.json"

Тестирование производилось на Kali Linux (чистая + уязвимая).

